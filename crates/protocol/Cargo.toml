[package]
name = "r-protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
r-crypto = { path = "../crypto" }
serde = { version = "1.0", features = ["derive"] }
bincode = "3.0"
thiserror = "2.0"

[dev-dependencies]
hex = "0.4"
